package foure.dev.util.tunnelbasefinder;

public enum MendStage {
   ENSURE,
   ROTATE_DOWN,
   OFFHAND_XP,
   THROW_XP,
   REOFFHAND_TOTEM,
   ROTATE_BACK,
   RESET;

   // $FF: synthetic method
   private static MendStage[] $values() {
      return new MendStage[]{ENSURE, ROTATE_DOWN, OFFHAND_XP, THROW_XP, REOFFHAND_TOTEM, ROTATE_BACK, RESET};
   }
}
